package project2;

public class Main {
    public static void main(final String[] args) {
        new Application().runApplication();
        // without this, the app does not finish
        System.exit(0);
    }
}